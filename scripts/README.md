
This note provides a basic description of the `scripts` folder.

1. `main.R` calculates the omega values across various items using the panel data,
2. `helpers/omega.R` features the main function for calculating our omega measure.