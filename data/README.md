
This note provides a basic description of the `data` folder.

1. `source_files` folder contains the cleaned-up panel data from household panels. Since these data require permissions from the respective organizations, the folder is empty in public.
2. `output/omegas.rds` provides the estimated omega measures across all items.